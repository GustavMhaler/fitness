# Protect the single-user application with a 30-day access gate

Although the application serves one user and has no account system, its deployed URL exposes personal training data and write operations. The application will therefore require a shared access passcode and remember successful access for 30 days, preserving the simple single-user experience without leaving the public deployment unauthenticated.
